<form action="/login">
<%= hidden_field_tag :authenticity_token, form_authenticity_token %>
<button id="login-btn">Log in</button>
</form>
<div id="splash">

    <p id="explore">[  <a href="#">Explore Activity Share!</a>  ]</p>

    <%= image_tag "Logo.png", :class => "logo grow" %>
    <h1 class="grow">Activity Share</h1>
    <form action="/activities">
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
      <input id="place-input" type="text" placeholder="Enter Location" name="location">
      <button>Search</button>
    </form>
</div>

<script>

  $(document).ready(function() {

  var input = $('#place-input');
  var searchBox = new google.maps.places.SearchBox(input[0]);
    
  });


</script>